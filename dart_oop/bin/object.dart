class Student {}

void main() {
  // actually every part in dart are coming from object
  // object class is directly automate instantiate when we define a class
  
  const number = 100;
  print(number.toString());

  var student = Student();

  print(student.toString());
}
